package JavaLar;

public class Python extends Planetas {

	public Python(int velocidade, int horas) {
		super(velocidade, horas);
		
		setHoras(0);
		setVelocidade(4);
		setPosicao(new int[] { 8, 9 });
	}

	public int horasPassadas(int instantes) {

		horas = 24 * instantes;

		return horas;
	}

	public void movimento(int instantes) {

		
		for (int i = 0; i < instantes; i++) {

		}

	}
}
